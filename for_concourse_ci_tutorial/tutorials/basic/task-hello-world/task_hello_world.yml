---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: resource-tutorial
run:
    path: sh 
    args: 
      - -exc
      - |
        hostname
        whoami
        env
        pwd
        find /tmp/
        ifconfig -a
        hostname > resource-tutorial/oldhost.txt
